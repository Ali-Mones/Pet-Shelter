export type Filter = {
    species?: string[],
    breeds?: string[],
    minAge?: number,
    maxAge?: number,
    shelterLocations?: string[],
    houseTraining?: boolean,
    spayedNeutered?: boolean,
}
